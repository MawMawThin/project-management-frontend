# Copy to .env.production before building, or set in Netlify/Cloudflare env vars.
# Replace with your real backend URL (no trailing slash after /api is fine — include /api).
VITE_API_URL=https://YOUR-BACKEND.onrender.com/api
